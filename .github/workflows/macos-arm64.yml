name: macOS arm64 build (Craft)

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Xcode tools
        run: xcode-select --install || true

      - name: Setup Python (non-Homebrew)
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install KDE Craft (force arm64)
        run: |
          python -V
          python -m pip --version
          curl -L https://raw.githubusercontent.com/KDE/craft/master/setup/CraftBootstrap.py -o setup.py

          # CraftBootstrap on macOS defaults to x86_64.
          # Feed "1" to select arm64, then press Enter for the next prompt(s).
          printf "1\n\n" | python setup.py --prefix "$HOME/CraftRoot"

      - name: Load Craft environment
        run: |
          source $HOME/CraftRoot/craft/craftenv.sh
          craft --version

      - name: Build KomoDo with Craft
        run: |
          source $HOME/CraftRoot/craft/craftenv.sh
          craft komodo

      - name: Package (DMG)
        run: |
          source $HOME/CraftRoot/craft/craftenv.sh
          craft --package komodo

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: komodo-macos-arm64
          path: |
            $HOME/CraftRoot/build/**/image-*/**/*.dmg
            $HOME/CraftRoot/build/**/image-*/Applications/**/*.app
